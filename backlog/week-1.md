# AI Guard - Sprint Backlog - Week 1

**Version**: 1.3
**Status**: Updated — Spec 04 selesai
**Date**: 7 September 2026
**Source**: PRD_AI_Guard.md v1.0

---

| Tahap | Output | Catatan |
|---|---|---|
| Minggu 1 (31/08/26) | *Overview* kendala dari project | **Poin Temuan**: <br> <ul> <li> Kelas yang dapat diprediksi hanya bersifat biner yaitu kelas anomalous dan normal saja, jika kelas anolamous mau dijabarkan lagi jadi serangan apa saja kayaknya cukup sulit karena kelasnya pasti terlalu banyak. </ul> |
| Minggu 1 (01/09/26) | Menjalan *initial check* training notebook 01-xgboost-training.ipynb | **Poin Temuan**: <br> <ul> <li> Notebook [01-xgboost-training.ipynb](kompres\notebooks\01-xgboost-training.ipynb) berhasil dijalankan <li> Model [xgboost (.pkl)](kompres\models\01-xgboost-training\01-xgboost_model.pkl) berhasil dibuat, metrik evaluasi dapat dilihat pada file [metadata](kompres\models\01-xgboost-training\01-xgboost_metadata.json). <li> Secara metrik sudah cukup bagus, namun perlu dibuktikan pada production. <li> Jika ingin lanjut develop aplikasi web, input model harus sesuai format! lihat format pada notebook/metadata dan lakukan preprocessing (feature extraction) data traffic sesuai yang dilakukan di notebook. Ini supaya bisa masuk ke modelnya</ul> |
| Minggu 1 (04/09/26) | Implementasi & verifikasi Spec 03 (Inference API) | **Poin Temuan**: <br> <ul> <li> Endpoint `/predict`, `/predict-batch`, `/health`, `/metrics` berhasil diimplementasi dan diuji end-to-end via curl, seluruhnya lolos dengan latensi rata-rata < 2ms (target < 100ms) <li> Ditemukan bug fatal di contoh kode Spec 03: `query_params` di-hardcode kosong padahal menyumbang >61% feature importance — sudah diperbaiki dengan parsing riil dari query string & body form-urlencoded <li> `attack_class` tidak bisa berbasis ML karena model biner murni; diimplementasi sebagai heuristic signature-count sementara <li> **Ditemukan false positive**: model CSIC 2010 salah mengklasifikasikan request modern sederhana (`GET /home`) sebagai anomalous akibat domain gap — ditambal dengan guardrail manual di API; sudah diverifikasi end-to-end di Spec 04 (Test 1: `GET /home` → 200 `X-AI-Guard: allow`); retrain di level model tetap utang jangka panjang <li> Perlu keputusan sebelum Step 4: apakah retrain model dengan data traffic modern, apakah attack_class tetap heuristic atau upgrade ke ML multi-class, dan apakah inference API jalan sebagai microservice terpisah atau in-process dengan middleware </ul> |
| Minggu 1 (06/09/26) | Implementasi & verifikasi Spec 04 (Middleware Integration) | **Poin Temuan**: <br> <ul> <li> **Middleware + PolicyEngine + StubDBLogger** selesai & terverifikasi: Flask website (`src/website/app.py`), AIGuardMiddleware (`src/middleware/ai_guard_middleware.py`), PolicyEngine, StubDBLogger semua berjalan <li> **4 test end-to-end PASS**: (1) Normal `GET /home` → 200 + `X-AI-Guard: allow`, (2) SQLi `GET /search?q=test' OR 1=1--` → 403 blocked, (3) Fallback saat Inference API mati → 200 (tidak crash), (4) Skip path `/health` → 200 tanpa header `X-AI-Guard` <li> **3 unit test PolicyEngine PASS**: normal→allow, anomalous ≤0.7→flag, anomalous >0.7→block <li> **HTTPRequestParser dihapus dari middleware** — tidak pernah dipakai di extract_request_data(); Inference API menangani sendiri <li> **StubDBLogger** sebagai placeholder; interface `log_request()` identik dengan TrafficLogger (Spec 05) untuk swap tanpa ubah kode middleware <li> Detail: lihat `implementation04-report.md` </ul> |
| Minggu 1 (07/09/26) | Revisi & hardening Spec 04 (7 langkah perbaikan) | **Poin Temuan**: <br> <ul> <li> **Benchmark latensi 100 request** (50 normal + 50 attack) dicatat: P50 ~2.2ms, P95 ~4ms, P99 ~28ms (cold) / ~3ms (warm) — semua < 100ms ✅ (lihat `implementation04-report.md` §7) <li> **Klaim guardrail dikoreksi**: guardrail `is_clean_benign` sudah terbukti end-to-end di Spec 04 Test 1; retrain model tetap utang jangka panjang <li> **Multi-env config**: `inference_api_url` sekarang ENV-driven (`INFERENCE_API_URL`); default lokal, override via ENV untuk Docker/staging <li> **Guardrail `is_clean_benign` diamankan dengan 4 regression test** (`src/api/test_clean_benign_guardrail.py`): total 12 test di `src/api/` + `src/middleware/` semua PASS <li> **Konfigurasi middleware masih hardcode** di `config/middleware_config.py`; didokumentasikan di `docs/HARDCODED-CONFIG.md`; migrasi ke tabel `configuration` SQLite ditugaskan ke Spec 05 <li> **Known limitation `X-AI-Guard` header** tidak ada pada response 403 (block) — didokumentasikan di `docs/KNOWN-LIMITATIONS.md`; keputusan: dibiarkan karena body 403 sudah self-explanatory <li> **`data/*.jsonl` masuk `.gitignore`**; `data/.gitkeep` dibuat agar folder tetap di-track </ul> |

